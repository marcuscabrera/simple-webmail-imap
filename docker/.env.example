# ===========================================
# WebMailForcenecedor - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e configure os valores

# Flask
FLASK_ENV=production
SECRET_KEY=sua-chave-secreta-muito-segura-mude-isso

# PostgreSQL
POSTGRES_USER=webmail
POSTGRES_PASSWORD=webmail123
POSTGRES_DB=webmail_db

# Servidor de Email (Postfix/Dovecot)
IMAP_HOST=mail.seudominio.com
IMAP_PORT=143
SMTP_HOST=mail.seudominio.com
SMTP_PORT=25

# Redis (opcional)
REDIS_URL=redis://redis:6379/0
